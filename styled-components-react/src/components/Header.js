import React from "react";
import { Button } from "../Buttons";

const Header = () => {
  return (
    <div>
      <h1>Hello from Header</h1>
      <Button>I'm from Header</Button>
    </div>
  );
};

export default Header;
